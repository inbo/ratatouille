% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_object_ids.R
\name{list_object_ids}
\alias{list_object_ids}
\title{List all object ids for a resource on a ArcGIS Enterprise MapServer}
\usage{
list_object_ids(source, resource = get_default_resource(source))
}
\arguments{
\item{source}{(Required) Character string indicating the source of the data
to fetch. Currently supported data sources: \code{rato} will fetch RATO data and
\code{wfl} will fetch data from the province of West Flanders.}

\item{resource}{The resource to query. Defaults to the default resource for
the given source.}
}
\value{
Integer vector of (all) object ids.
}
\description{
This function lists all object ids for a given resource on a ArcGIS
Enterprise.
}
\section{Fetching all objects}{


Downloading all data for a resource consists of a two step process. First
all object ids for a resource are queried. Then the objects are fetched
from that resource by their id.
}

\section{Caching}{


The returned id's are cached by default, but this can be overwritten by
calling \code{memoise::forget(list_object_ids)} which will remove the cache and
ensure the next call queries the database (but will reinstate the cache for
any follow up calls).
}

