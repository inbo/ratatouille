% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_object_ids.R
\name{query_object_ids}
\alias{query_object_ids}
\title{Query object ids for a resource on a ArcGIS Enterprise MapServer}
\usage{
query_object_ids(
  source = c("rato", "wfl"),
  resource = get_default_resource(source),
  query = "1=1"
)
}
\arguments{
\item{source}{(Required) Character string indicating the source of the data
to fetch. Currently supported data sources:\code{rato} will fetch RATO data and
\code{wvl} will fetch data from the province of West Flanders.}

\item{resource}{The resource to query. Defaults to the default resource for
the given source, but can be overwritten by any resource returned by
\code{list_resources()}.}

\item{query}{A SQL where clause to filter the objects to query. Defaults to
"1=1" which queries all objects, but can be used to filter the objects to
query by any field in the table. For example, if you only want to query
objects with a specific value in a field, you can use a query like
"field_name = 'value'".}
}
\value{
Integer vector of (filtered) object ids.
}
\description{
This function returns all object ids that pass a query for a given resource
on a ArcGIS Enterprise.
}
\examples{
query_object_ids("wfl",
                 query = "Gemeente='Koekelare'")
  
}
